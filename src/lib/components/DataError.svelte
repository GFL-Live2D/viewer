<div class="bg-background flex min-h-screen items-center justify-center px-6">
    <div class="max-w-lg text-center">
        <h1 class="mb-2 text-2xl font-bold text-red-400">500</h1>
        <p class="text-foreground-tertiary text-sm leading-relaxed">
            No Live2D assets found.
            <code class="bg-background-secondary rounded px-1 py-0.5 text-xs">PUBLIC_ASSET_BASE_URL</code>
            is not set and
            <code class="bg-background-secondary rounded px-1 py-0.5 text-xs">static/assets/</code>
            is empty. If you just ran
            <code class="bg-background-secondary rounded px-1 py-0.5 text-xs">bun install</code>
            +
            <code class="bg-background-secondary rounded px-1 py-0.5 text-xs">bun run dev</code>, you still need to
            supply model assets: either populate
            <code class="bg-background-secondary rounded px-1 py-0.5 text-xs">static/assets/</code>
            (see "Asset layout" in
            <code class="bg-background-secondary rounded px-1 py-0.5 text-xs">README.md</code>), or set
            <code class="bg-background-secondary rounded px-1 py-0.5 text-xs">PUBLIC_ASSET_BASE_URL</code>
            to point at a CDN hosting them. See
            <code class="bg-background-secondary rounded px-1 py-0.5 text-xs">README.md</code>
            "Getting Started" for the full steps.
        </p>
    </div>
</div>
