[project]
name = "gfl-live2d-viewer"
version = "0.1.0"
description = "Live2D model extraction and conversion scripts"
requires-python = ">=3.10"
dependencies = [
    "yamlium>=0.1.14",
    "pyyaml>=6.0",
    "boto3>=1.26.0",
    "python-dotenv>=1.2.3",
]
